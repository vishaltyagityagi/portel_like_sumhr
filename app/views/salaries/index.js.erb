$('#container').html("<%= escape_javascript (render partial: 'index', locals: { salary: @salary}) %>");
document.title = "Projects"
if (history && history.pushState) {
	history.pushState(null, document.title, "<%= salaries_path%>");
	$(window).bind("popstate", function(){
    $.getScript(location.href);
  });
}
