$('#container').html("<%= escape_javascript (render partial: 'form', locals: { employee: @employee}) %>");
document.title = "Projects"
if (history && history.pushState) {
	history.pushState(null, document.title, "<%= root_path%>");
	$(window).bind("popstate", function(){
    $.getScript(location.href);
  });
}
