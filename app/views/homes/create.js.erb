$('#container').html("<%= escape_javascript (render partial: 'index', locals: { leave: @leave, leaves: @leaves}) %>");
document.title = "Projects"
if (history && history.pushState) {
	history.pushState(null, document.title, "<%= homes_path%>");
	$(window).bind("popstate", function(){
    $.getScript(location.href);
  });
}
